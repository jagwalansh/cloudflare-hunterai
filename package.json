{
  "name": "hunterai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend && npm run build:worker --workspace frontend",
    "deploy": "cd frontend && npx wrangler deploy",
    "start": "npm run start --workspace frontend"
  }
}
